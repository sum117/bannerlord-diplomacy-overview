@AGENTS.md

## Claude-specific notes

- Session title codename for this repo: `DIPLOVIEW` (e.g. `DIPLOVIEW(FEATURE[UI]) relations tab skeleton`).
- Subagent/model discipline: use `haiku`/`sonnet` for lookups and mechanical extraction; reserve the
  top-tier model for synthesis and hard design/coding surfaces.
